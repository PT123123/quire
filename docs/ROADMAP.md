# Roadmap

Milestone plan from `docs/SPEC.md` with current status. Living status
notes and per-milestone reports live in `PLAN.md`; this file is the
stable map. Priorities (SPEC §三十五): UI quality > editing experience >
low RAM > low CPU > GPU rendering > maintainability > feature count.

| Milestone | Scope | Status |
|-----------|-------|--------|
| M0 Toolchain | Rust + Slint 1.18, MSVC, renderer matrix, benchmark harness, baselines | ✅ 2026-09-18 |
| M1 Design system | Theme/Colors/Typography/Icons, product-grade shell (mock data) | ✅ 2026-09-18 |
| M2 Shell navigation | Live workspace tree (create/rename/duplicate/delete/favorites/recents), page switching with per-page mock content, Ctrl+P search, context menus, settings + confirm dialogs, empty state, headless visual-regression tool, scenes F/G benchmarks | ✅ 2026-09-19 |
| M3 Local documents | `core/` document model, SQLite storage (workspaces/pages/blocks/metadata), debounced batched persistence, transactions, load-on-restart, settings persistence | ✅ 2026-09-19 (wired same day) |
| M4 Block editor MVP | Command system + undo/redo, single-TextInput editing, Enter/Backspace/merge/split, arrows, clipboard, 9 block kinds, Chinese IME acceptance pass | ✅ 2026-09-19 core · ✅ IME pass 2026-09-20: the user walked `docs/IME_CHECKLIST.md` by hand and it is now fully checked, which was M4's last open item and M8's last non-code gate |
| M5 Notion interactions | Slash menu (command descriptors from Rust), palette wiring on the real command registry, block drag/reorder, block menu | ✅ 2026-09-19 (drag/reorder = ADR-0021; markdown shortcuts + curated slash/Turn-into menus = ADR-0022; collab/AI/color/copy-link stay out per SPEC) |
| M6 Rich text | Inline spans (bold/italic/code/link/strike), mark persistence, format-safe save/load | ✅ 2026-09-19 (link UI + import/export marks in; run rendering has a documented wrap limitation) |
| M7 Performance | Virtualization depth, lazy loading, background search (FTS), long-document suite at 1k/5k/10k blocks | ✅ 2026-09-19 · FTS5 index + async search, and the femtovg/skia scene A–G matrix is recorded in `docs/PERFORMANCE.md` (SPEC §六 checklist: all met); the release profile that produced these numbers is pinned by the A3 audit as ADR-0024 |
| M8 Windows RC | Crash recovery, migrations, packaging/installer, Markdown import/export, settings UI, keyboard shortcut pass | 🔄 in progress 2026-09-20 · delivered: rotating snapshots + startup recovery (ADR-0015), the `session.meta` + rotating log + panic hook and the clean-exit marker (ADR-0018), data-location move to the per-user library (ADR-0020), schema migrations to v5, Markdown import/export, installer + `just dist`, LAN share/pull, settings UI incl. the STORAGE row (data folder / open-folder / back-up-now), Page and Link-to-page blocks, rich clipboard paste, Move page, the shortcut pass, Go Back / Go Forward (SPEC §十六's last palette item that had no implementation: an in-session page history behind two `Navigate` rows and Alt+← / Alt+→, and because every navigation now commits the debounced typing edit first, a page switch can no longer swallow the last 300 ms of typing). The tail A-package is landing: A1 `--portable` ✅, A3 release-profile audit ✅ (pinned as ADR-0024), A5 this refresh, A2 first-paint measurement ✅ plus its phase-split follow-up (the old window-up number understated startup ≈4×; the remaining gap is now owned per phase — a flat ≈410 ms inside `ui.run()` before the first frame, document-independent, and one scaling term `state_new` 5→148 ms; see `docs/PERFORMANCE.md`), a second follow-up that measured the scaling term at four points in one batch and settled it as **linear** — ≈4 ms + 13.1 ms per 1 000 blocks, `repo_open` flat at 51 ms across a 20× document range, and `benchmarks/scripts/audit_results.ps1` now regenerates every table in PERFORMANCE.md from its raw rows), a third that closes the skia question — one batch, both arms from the same source: skia (`--no-default-features --features skia-opengl`) paints 44 ms sooner on an empty shell (440 vs 484 ms) and ties on a 10 000-block page, so it is not worth its ≈2× working set and **femtovg stays the default**; the plain `--features skia` build cannot be measured this way at all — its rendering notifier is silent (`Surface::with_graphics_api` is a no-op on that surface), and `--features skia` without `--no-default-features` does not even disable the femtovg default (rows `benchmarks/results/2026-09-20-first-paint-ab-*`), A6 installer end-to-end ✅ (`install/verify-installer.ps1`; re-run green at `6c115b5` with the current release exe — 7.94 MB setup, installed exe exits 0, embedded icon still pixel-matches `quire.ico`, the `--open` verb imported pages 14→15, uninstall left zero residue), A1 follow-up: `--portable` verified end to end ✅ (`install/verify-portable.ps1`, 24/24, each child run with its own redirected APPDATA so the real per-user library is never in play), A4 visual sweep ✅ (34 scenes light + dark at `42cb158`, 13 pass / 21 fail, report in `.scratch/sweep/report.md`, harness `benchmarks/scripts/sweep.ps1`), re-swept at `6c115b5` after Track A's two fix commits — delta report `.scratch/sweep2/report.md`, method: hash every PNG against the first sweep, so only the 11 scenes that actually changed pixels were re-judged. Fixed: Settings' shortcut overflow, the Move-to popup clamp, the slash description column, the insert menu's disabled rows, find's counter, the dialog's destructive colour. Two of my own findings are retracted on that hash diff (bg-colour swatches, the title-edit line box) — both were 1:1 eyeball misreads of unchanged pixels. Re-swept a third time at `81a2937` (delta report `.scratch/sweep3/report.md`, same hash-diff method: 5 of 34 scenes moved, 29 carried their verdict unchanged) and the A4 LOW batch closed — the Add-link dialog's ~50 px dead zone (the popup height double-counted its own layout padding, so the VerticalLayout spent 32 px of slack as one gap: card 174 px → 141 px, gaps back to the designed 12/16 px, diff bbox exactly the old popup), mid-word snippet elision (both snippet builders now drop the partial word at each edge and leave CJK alone; the shot exercises the repo-less local path, which is why an FTS-only fix would have left the PNG byte-identical), the background-colour swatch rings, the notice band that had pushed the frameless title bar off the window edge (now y=40..69 under the 40 px bar), and the `recovered` fixture's invented copy citing `appdata/quire.db.bak1` — a path shape that predates ADR-0020 — which now mirrors the string `main.rs` really shows. A third of my own framings died in that re-sweep: the notice band does not "clip the sidebar's last row", because the tree list folds mid-row in the default scene too. A4 tail batch closed 2026-09-21 (ADR-0033, ADR-0034): the empty state's MEDIUM was worse than its copy admitted — a page with no blocks has nothing for any insert command to anchor on (every one of them takes a `BlockId`, and neither `create_page` nor `open_page` seeds a row), so a brand-new page could not be typed into at all. `Command::AppendBlock` + `AppState::start_page()` let the page write its own first paragraph, from a click on the empty panel or Enter in the title, one undo step, refused for container kinds. The two LOWs beside it are fixed: modals (delete confirm, Settings, Add link) now sit on `Colors.scrim` while anchored popups deliberately stay undimmed — the rule is in `docs/UI_ARCHITECTURE.md` §"Overlay layering" — and Toggle Sidebar moved to Ctrl+\, so Ctrl+B means only bold; Settings lists the chord now, which also retires that dialog's stale "falls below the bottom edge" limitation. The palette dispatch got the durable repair this section asked for: `palette_action()` + a no-wildcard `match` + a test over `mock_commands()`. Re-swept against `.scratch/sweep12` into `.scratch/sweep13` (44 scenes): 38 byte-identical, and each of the 6 that moved has a bbox that explains it — `empty` 724 sampled px inside x 506..1032 / y 456..466 (the copy line only), `palette` 7 px (the new hint chord), `link`/`dialog`/`settings`/`dark-link` whole-window (the scrim; `settings` is additionally one ShortcutRow taller) — plus a `--scene empty --click 640,458` shot that renders the page title and one focused empty row where the panel used to be. No RAM bench owed: this batch adds no block kind, no model field and no per-row state. Still open on the A4 list: 1 HIGH (a paragraph carrying inline marks loses its word wrap and is clipped mid-word, while the identical unmarked paragraph wraps; the "overprint" half of the original finding was the scene fixture's hardcoded mark offsets, now derived from the text, so the inline-code run is off-screen and untested — and the wrap itself is already documented as a Slint platform wall in `docs/EDITOR_ARCHITECTURE.md` §"Platform wall", i.e. design debt rather than a regression), 1 MEDIUM (find counts 16 matches and paints none of them on a blurred block — its selection-based hit routing is the same documented wall, so this one needs a focused manual look before it is called a defect), the rest of the LOW bundle (dimmest-text hint rows in both themes, weakest colour pairs) and one known limitation (`renderer_name()` and `renderer_id()` are compile-time, so a `--features software` shot labels itself "FemtoVG · GL" — deduped to one function this round, but the feature ordering still lies for any build that picks its backend at runtime); menu-overflow polish (was the same root cause as the Move-to HIGH — the popup height clamp landed with it); Explorer drag-and-drop import is deferred by a platform limit (Slint 1.18 delivers no external file-drop events — revisit on upgrade or via a Win32 `IDropTarget` in `platform/`) |
| M9 Android | Shared core model + schema, adapted navigation/IME test plan (starts only after Windows is stable) | ⏸ parked by explicit user decision 2026-09-20 ("安卓先不做"), and still blocked on M8 shipping Windows-stable. Evaluated 2026-09-20 by measurement rather than reading (evidence `.scratch/m9/report.md`, corrections folded into `docs/ANDROID_NOTES.md`): Slint 1.18's `backend-android-activity-06` + its Android renderer + bundled SQLite **compile and link for `x86_64-linux-android` on this machine's NDK 30** — a Quire-shaped `.slint` (window + `TextInput` two-way binding + popup) checks, and the `cdylib` links in 70 s warm. The one hard blocker in our graph is `rfd`, which has no Android backend and fails to *compile* (2 call sites, `controller.rs:1776`/`:1791`); the renderer there is **Skia/GLES, not FemtoVG** (Slint cfg-gates femtovg off Android), which made the desktop skia baseline in `docs/PERFORMANCE.md` a prerequisite rather than a curiosity — it is now measured, and note it is the **skia-on-OpenGL** arm that produced it, because the default desktop skia build's notifier never fires; the only environment read in the crate is `%APPDATA%`, whose fallback is a cwd-relative path, so an Android build without a supplied data dir runs silently in memory. UI cost measured: 31 hover affordances, 8 popups, 68 `UIState` properties, and row heights of 28–40 px everywhere except the 52 px search result row — that one row is the only interactive element meeting the 44 dp touch minimum. Residual risk is concentrated in upstream Android text input (11 open `a:platform-android` issues, incl. #9240 SwiftKey and #11810) and in **there being no device**: `adb devices` is empty and the local SDK has no `system-images/`. Recommended order is M9.pre (cfg-gate `rfd`, add `platform::data_dir()`, fix the feature list) → M9.0 input spike on hardware → M9.1 read-only viewer, i.e. the viewer is demoted from first because the desktop can now prove the part it was meant to de-risk. |
| M10 Block kit A+B | image (paste / insert / attachments / width steps), file + PDF thumbnail, simple table, toggle, columns | 🔄 in progress 2026-09-20 · SPEC §三十七 批次 A+B. Delivered: **toggle**, **image**, **file**, **table** and **columns** — 批次 B is closed. toggle — Schema v6 (`blocks.folded`, view state not content, so it is undoable through `Command::ToggleFold` and never bumps a document); the folded subtree produces **no realized rows at all** (`project_blocks` filters through `visible_block_indices`, the single seam shared with the new `drop_index_for_row`, so the §八 drag landing translates row→model instead of drifting); only a Toggle draws the chevron, so `SetBlockType` away from Toggle re-opens the section rather than stranding its children with no way back; `SplitBlock`/`DuplicateBlock` force `folded: false` because neither copies a subtree; Markdown export degrades to a quote line (the exporter is flat and has no subtree to assemble). image — Schema v7 (`attachments` table + `blocks.attachment`, no FK by design so a dangling reference loads as a missing picture instead of failing the library, + `blocks.img_percent DEFAULT 100`); bytes live in `<library>/attachments/<id>.<ext>`, extension and MIME sniffed from the content not the file name; anything over `MAX_EDGE = 1280` gets a second downscaled `<id>.cache.png` and the UI is only ever handed that copy, so viewing never degrades the original; the decoded rasters sit in an `AppState` LRU capped at 32 MiB weighted by RGBA bytes, because Slint's own path cache is 5 MiB — one photograph — and would re-decode every frame of a scroll; rows reach it through `image-for`/`image-aspect` **callbacks**, so cost tracks the viewport, not the document; undo drops the reference and never the bytes (ADR-0029). file — **no migration**: the kind reuses v7's `attachments` row and `blocks.attachment` column exactly, so `user_version` stays 7; `import_any_file` `fs::copy`s the picked file to `<library>/attachments/<id>.<ext>` and records only its length — no decode, no `image` call, no size cap, so a 2 GB attachment costs the same working set as a 2 KB one until the user asks for it (ADR-0030, §二十二); the row paints name (extension kept, unlike a picture's stem) + a right-aligned size from the `attachment-size` **callback** rather than a model field, and two always-drawn buttons, Open via `platform::open_with_default` (hand-declared `ShellExecuteW`, because `explorer.exe <path>` returns 0x1 even when it launched) and Save-as via `rfd` + `export_to`; Markdown writes `[name](quire://attachment/<id>)` — the *link* form, so unlike a picture the reference survives an export/import round trip. table — Schema **v8** (`blocks.columns`, 0 = not a table); the cells are `table_cell` **child blocks** in row-major order, so the row count is *derived* (`cells / columns`) and adding a row is `columns` ordinary `BlockInserted` changes — one undo step, no table-specific history (ADR-0031). The grid is one delegate: `visible_block_indices` hides cells the way ADR-0028 hides a folded subtree, so a table costs one realized row however large it gets. Tab / Shift+Tab walk the grid and a step past the last cell appends a row; the hover strip adds and deletes at the caret's row/column, and the plan refuses to take a table below 1×1 or to edit a ragged grid; Turn into runs both ways (the line's words land in the top-left cell, flattening gives every cell back as a paragraph); Markdown exports GitHub-flavoured tables, and import deliberately leaves `|a|b|` as text. Gate per kind: all six wiring points land (types / storage kind string / Markdown in+out / Turn into / slash / screenshot scene) and the 10 000-block scene stays ≤1.2× the current RAM baseline — re-measured at this commit (raw rows `benchmarks/results/2026-09-20-m10-image-ram.jsonl`): A idle 116.5–116.6 MB WS / 88.1–88.9 private, D10000 idle 126.4–126.5 / 100.9–101.3, i.e. **1.02× and 1.04× the M7 matrix rows** (114.3 / 88.2 and 121.5 / 97.4), idle CPU 0.2–0.59%. This measures the no-picture regression baseline — it proves the v7 columns and the cache fields did not make everything else dearer, not that pictures are cheap; a 10 000-block page **with** pictures scrolled is still unmeasured, and the 32 MiB decode ceiling is a construction bound plus a unit test, not a reading **(that debt closed 2026-09-21 by ADR-0036, below)**. The `file` slice re-ran the same two arms (raw rows `benchmarks/results/2026-09-20-m10-file-ram.jsonl`): D10000 126.5–126.6 / 101.1–101.3, i.e. the image batch's number to within 0.2 MB, and A 119.2–120.7 / 91.7–92.5 — 1.05×, a +2.6…4.1 MB shift on the empty shell that neither run contains an attachment to explain, so it is recorded unattributed in `docs/PERFORMANCE.md` rather than claimed away. The `table` slice ran the same two arms on the shipped build (raw rows `benchmarks/results/2026-09-20-m10-table-ram.jsonl`): D10000 130.8–131.0 / 104.5–105.7 = **1.08×**, A 124.8–125.3 / 96.7–98.3; both arms sit ~4–5 MB above the `file` batch's, by nearly the same amount, which is recorded as session drift rather than as the table's cost (an empty shell contains no grid). It also carries the one attributable A/B in the batch: guarding the row's `table_cells` model on `kind == Table` — instead of scanning the page and allocating an empty `VecModel` for all 10 000 rows — took D down ≈2 MB WS / ≈2.5 MB private while leaving A flat, which is the control that says the saving is per row and not per session. columns — **no new migration**: the layout stores its box count in v8's `blocks.columns`, a box is a `column` child block and a line in a box is that box's child, so undo, redo and §十八 need to know nothing about layouts (ADR-0032). Slint has no recursive components, so the layout's content is drawn inside the layout's **own** delegate — `visible_block_indices` hides boxes and their lines, which is what §三十七's "分栏只在可见窗口内展开" asks for and what §十二's virtualization premise requires; the delegate gets one flat `[ColumnItem]` plus a `[ColumnBox]` shape table it slices, because QOML cannot filter a repetition. Tiling is `FlexboxLayout` (`no-wrap`, `alignment: stretch`, one `horizontal-stretch` per box), not hand-written widths; the hover strip adds a box at the right end and deletes from it, refused below two and above three, and a deleted box's lines re-key onto the box before. An empty box is the one thing a click cannot put a caret in, so it says so and `ColumnsAddBlock` gives it its first paragraph. Markdown flattens the layout to page-level paragraphs (words and reading order kept, shape lost). Two things this slice fixed on the way, both shared with the grid: `InsertBlockAfter` used to force `parent: None` and key off the container's own row, so a "+" or "Paste below" could wedge a top-level block inside a container's subtree — and a page is one flat slice sorted by `order`, so that breaks the subtree contiguity every index reader depends on; and the "/" popup anchored itself with a hardcoded 350 px window reserve, which the menu outgrew the moment it gained a kind, so it now measures its own height like the "+" menu already did. Gate re-measured on the shipped build (raw rows `benchmarks/results/2026-09-20-m10-columns-ram.jsonl`): D10000 135.5–137.0 / 109.9–110.7 = **1.13×**, A 126.5–127.0 / 98.4–99.6 = 1.11× — inside the ≤1.2× gate, and the fourth batch running the same direction on both arms, which is now written up as a method finding: the only per-row cost this kind can add to a page with no layout on it is two 8-byte model pointers (`ModelRc::default()` is `None` in Slint 1.18 and allocates nothing), ≈160 KB across the bench page against the ≈3.5 MB that D − A grew, so the drift stands unattributed and the next kind owes a same-session control build. The sweep grew to 44 scenes (`.scratch/sweep12`): `columns` and `columns-3` are the tiling pair, 1 111 differing pixels all inside the layout's own band, and the three menu scenes moved because the list gained a row. Remaining: 批次 A's PDF first-page thumbnail (deferred by explicit user instruction 2026-09-20; renderer route undecided — until it lands a `.pdf` and a `.zip` differ only by name). Orphaned-attachment GC was the other open item and it is **delivered 2026-09-21** (ADR-0037): Settings → STORAGE → **Reclaim** deletes the `attachments` rows nothing reaches — every page's blocks, both undo stacks up to the 100-step cap, the copied block — then their files, and it deliberately never lists the folder, because a session whose attachment load failed would otherwise "see" zero references and delete the library. 批次 A's clipboard-bitmap paste is delivered 2026-09-21 (ADR-0035): Ctrl+V with a screenshot on the clipboard stores it through the same `import_bytes` path as a picked file, `CF_DIBV5`/`CF_DIB` decoded by `platform/dib.rs` — hand-declared FFI per ADR-0025, the decode kept out of the Win32 file so it is testable without a clipboard — text outranking image when a copy carries both, and an empty block *becoming* the picture rather than leaving a stray line. No RAM bench was owed for it (no new kind, no model field, no per-row state) and the sweep moved 0 of 44 scenes; what it did measure is the paste's own latency, in `docs/PERFORMANCE.md`. **ADR-0036** then paid the picture-scroll debt: `--pictures N` turns every `rows/N`-th bench row into an image block over a pool of 200 generated 1280×720 fixtures seeded only on a fresh library (so a measured pass loads rather than re-encodes), and the app reports its own cache on stderr under `--dump-state`, because a working-set counter cannot see a cache whose unit is one raster. Readings (raw rows `benchmarks/results/2026-09-21-m10-media-ram.jsonl`, nine scenes one sitting): the 32 MiB ceiling **binds at 9 rasters / 33 177 600 bytes (98.9 %)** in every scrolled media arm whether the page holds 500 or 5 000 pictures; 500 pictures on a page you never scroll to are indistinguishable from none (0 decodes, D+500 = D); and a picture *on screen* costs ≈9 MB of process memory against its 3.7 MB raster — the viewport is the binding constraint, not the cache, so the ceiling stays where it is. The gate itself re-ran same-session: D 1.12× / A 1.10×, with the media arms deliberately outside it. **And it invalidated the harness:** Slint's list content offset is negative going down, so scene F's timer had been adding into a clamp ever since M2 — the continuous-scroll numbers on record measured 60 property writes a second at offset 0, and the `--scroll-y` pixel control proved it (`0` and `+2000` byte-identical PNGs, `−2000` differs). Real scroll is now 36.5–38.4 % of a core at a wheel tick and 83–88 % at a flick, and every pre-2026-09-21 scroll number in `docs/PERFORMANCE.md` is marked for re-reading. Next is 批次 C (highlight / bookmark / embed / math / TOC / synced block), per SPEC §三十七. The same day, `benchmarks/scripts/scroll_ab.ps1` paid the follow-up the fix created: the corrected scroll measured against **both renderers in one sitting**, each binary pre-flighted against its own reported renderer so a skia build that kept the femtovg default cannot pass for an A/B (rows `benchmarks/results/2026-09-21-m10-scroll-skia.jsonl`). skia/GL is still cheaper to scroll — ≈18 % at a wheel tick, ≈21 % at a flick, not the third the M7 row claimed — for ≈+34…43 MB working set, so **femtovg stays default** and the verdict's skia sentence is now proven rather than inherited; the 9-raster ceiling came out renderer-independent (identical `peak_bytes` on both arms), because the LRU sits above the renderer. Reproducing those rows through the matrix is what caught a third instrument bug: `-Only A,B` reaches a `[string[]]` parameter as **one** string under `powershell -File`, so a multi-token filter matched no scene, wrote no rows and exited 0 — it now splits the tokens and throws on an empty match, and the matrix gained the two shapes the batch had run by hand (the flick on a text page, a short page of pictures) so every published row is reproducible from the script. One defect the table slice surfaced and fixed on the way: `DocumentRow.head` bound its `height` and left `y` alone, and Slint centres such a child vertically, so the page title had been rendering ~34 px below its binding in every scene since M1 — with a tall first block it was ~60 px and the grid painted over it. That fix re-baselined 37 of the 42 sweep scenes (`.scratch/sweep10`, all 37 diffs confined to the title band), and the trap is written down in `docs/UI_ARCHITECTURE.md` §"Slint geometry traps". Can interleave with the M8 tail; M9 stays parked. |
| M11 Block kit C | code highlighting, bookmark, embed card, math, TOC | 🔄 in progress 2026-09-21 · SPEC §三十七 批次 C. Delivered: **math** (ADR-0038) — `BlockKind::Math` (row id 20) and `MarkKind::Math`, both storing LaTeX-subset **source** and rendering it to Unicode through one function, `core::math::to_unicode`. **No migration** (kinds and mark kinds are strings in the database, so `user_version` stays 8) and no layout engine, which is what keeps §三十七's "要引入排版引擎必须先出 ADR 并附内存数字" gate untriggered. The block row asks for its glyphs through a `pure callback math-render`; the inline runs convert at projection time instead of in a binding, because a binding pays per repaint — and the block's binding is kind-guarded (`is-math ? … : ""`), because `visible: false` does not stop a binding from running: ≈0.61 µs per formula measured, so an unguarded row is ≈6 ms per projection on a 10 000-line page. The renderer's contract is three rules with tests on each: the output never loses what the user typed (an unknown command and a `\begin{…}` echo their own source), whitespace in the source is content and not syntax, and the render is idempotent. Markdown: `$$ … $$` fences verbatim the way code fences are, `$…$` behind TeX's flanking rule so "costs $5 and $10" stays prose, and the exporter escapes a `$` only when a pair could really re-form on the way back. Ctrl+M marks a selection in all three editors (block, table cell, column line), `$$ ` converts a line, the kind is in slash / Turn into / the "+" menu, and the settings dialog lists the chord. Gate: **1.016× the control's private bytes** on scene D — and this is the batch that pays 批次 B's debt: the previous commit built in a clean worktree, arms alternating in one sitting, each identified by md5 and size (raw rows `benchmarks/results/2026-09-21-m10-math-ram.jsonl`). The sweep grew 44 → 46 scenes (baseline `.scratch/sweep21`): `math` and `math-inline` added, 4 existing scenes moved with a bbox that explains each one, and `math-inline` caught a real defect on its first shot — the run `HorizontalLayout`s defaulted to `alignment: stretch`, so a marked line with leftover width painted its three runs ~155 px apart; `alignment: start` on all three run rows moved **0 of the 44** baseline scenes, because every one of theirs overflowed instead. Not done and not pretending: no display-style layout (`\frac{a}{b}` is one-line `a/b`), no environments, no math font, and no headless scene of a formula *being edited* — that and Ctrl+M need a human window. Remaining in 批次 C: highlight / bookmark / embed / TOC / synced block, of which **embed** (placeholder card + external open, no WebView) and **TOC** (derived from the page's headings; the jump already exists as a `quire://block` anchor) are unblocked, while highlight wants the single-line runs channel whose lost wrap is the documented platform wall, bookmark wants a TLS client, and synced block waits on §四十. Hard limits: no JS/WASM runtime, no WebView (SPEC §二/§三十三), and highlighting must not make long-code-block typing latency measurable. |
| M12 Page appearance | icon, cover, font (default/serif/mono), full width, small text, lock, version history, templates | ⏳ planned · SPEC §三十八. Needs a `pages` migration (schema +1, old libraries must upgrade losslessly); version history reuses SPEC §二十五's snapshot mechanism rather than new storage; a locked page must never silently swallow input. |
| M13 References | @page mention, @date, backlinks panel, synced block | ⏳ planned · SPEC §四十. References store ids, not titles; backlink index rides SPEC §二十's search index incrementally — no full-library scan on page open. |
| M14 Database | record+schema+view model; table → board → list → calendar → gallery → timeline → form → chart; properties, filter/sort/group, relation/rollup/formula, linked database, database templates | ⏳ planned · SPEC §三十九, the largest single item in the plan and the one real Notion gap. Starts only after M10 批次 B and M13. filter/sort in SQL not in the UI; 10 000 rows never fully realized; RAM + view-switch + formula-panel timings go to PERFORMANCE.md. |

## Explicitly out of scope for v1

Sync, collaboration, cloud, plugin market, AI, multi-process IPC, custom
TSF/IME implementation, publish-to-site, comments/discussions/reactions.

Everything else the SPEC had parked is now scheduled — SPEC §三十七–四十,
milestones M10–M14 above (decided 2026-09-20, ADR-0027). `image`, `table`,
`toggle` and `database` left this list on that date. `callout`, `Page` and
`Link` were never gaps: they shipped at ADR-0023/0026. Block kinds persist
as strings (`BlockKind::as_str`), 21 of them as of ADR-0038, so any kind
numbering in older prose is informal.

## Definition of done (per milestone)

1. Scope items implemented and wired through the controller (no mocks in
   the UI layer).
2. `just check` green: check + tests + release build.
3. Performance re-measured for affected scenes in `docs/PERFORMANCE.md`.
4. Visual scenes re-shot (`just shot`) and reviewed.
5. `PLAN.md` report written; `DECISIONS.md` updated for any non-obvious
   choice.

## M8 verification snapshot (2026-09-21, at `24a3432`)

The A4 tail batch (ADR-0033, ADR-0034) re-ran the gates it could move, and the
one gate the previous snapshot caught itself missing:

| gate | command | result |
|------|---------|--------|
| tests | `cargo test --all-targets` | green: 293 passed, 0 failed, 4 ignored across 10 binaries (+82 over `03e0427`; this batch adds 5) |
| release build | `cargo build --release` | clean, **zero warnings** (re-confirmed by touching the four changed sources and re-checking `--release --all-targets`: 0 warning lines) |
| behaviour | `every_palette_row_resolves_to_its_own_action` | the command-registry test the section below called "still not written" now runs on every `just check` — this is the gate that would have caught `aaa3763`, and the visual sweep demonstrably never was: the whole batch moves 7 sampled pixels in the `palette` scene |
| visual | `sweep.ps1 -OutDir .scratch/sweep13 -Baseline .scratch/sweep12`, then `-OutDir .scratch/sweep14 -Baseline .scratch/sweep13` | 44 scenes: 38 byte-identical, 6 moved, each with a bbox that its own change explains (`empty` copy band only, `palette` hint only, 4 whole-window scrims); the follow-up copy edit then moved **0 of 44**, which is what makes "fixture text no scene renders" a reading rather than a claim |
| not re-run | performance audit, installer, portable | deliberately: this batch adds no block kind, no schema column, no per-row state and no build-profile change, so there is nothing for those gates to observe |

## M8 verification snapshot (2026-09-20, at `03e0427`)

There is no CI on purpose, so this is the manual "definition of done" evidence
for the current head, all of it re-runnable from the repo:

| gate | command | result |
|------|---------|--------|
| tests | `cargo test --all-targets` | green: 211 passed, 0 failed, 4 ignored across 10 binaries |
| release build | `cargo build --release` | clean, **zero warnings** (the four below are gone) |
| visual | `benchmarks/scripts/sweep.ps1 -OutDir .scratch/sweep4 -Baseline .scratch/sweep3` | 34/34 render; **0 of 34 PNGs changed** against `81a2937`, so every verdict carries over: the open list is still 1 HIGH + 2 MEDIUM + 3 LOW + 1 known limitation (`.scratch/sweep3/report.md`) |
| performance | `benchmarks/scripts/audit_results.ps1` | `audit ok`: 12 first-paint + 6 bench batches, every stored summary agrees with its raw runs |
| packaging | `install/verify-installer.ps1` | 4/4 green at this head: 7.94 MB setup, installed exe exits 0, embedded icon still pixel-matches `quire.ico` (384 px sample), `--open` imported pages 14→15, uninstall left zero residue |
| data placement | `install/verify-portable.ps1` | 24/24 green, real per-user library untouched |

### What the previous snapshot was warning about, and what its fix taught

The four build warnings (`unused variable: CMD_COPY_MD` plus two
`unreachable pattern`) were one defect and a functional one: a constant used
in a `match` arm without being imported makes that arm a catch-all *binding*,
so every palette command with an id ≥ 9 — the three Markdown actions and every
"Jump to page" row — ran `copy_current_page_markdown` instead of its own
action, and no page could be opened from the palette. It is fixed (`aaa3763`,
one name added to the import list at `src/app/controller.rs:9`) and the
release build is now silent.

Two things that fix demonstrated about this checklist are worth keeping,
because both are still true. (1) The test suite was green *because* nothing
walks the palette dispatch, so `just check` cannot catch this class of
breakage — the durable repair is a test over the command registry, not the
one-word import. That test now exists (ADR-0034): the ids resolve through
`palette_action()` into a `PaletteAction` enum, the controller's `match` has no
wildcard arm, and a test walks `mock_commands()` asserting every row resolves to
its own distinct action. The same commit that fixed the dispatch changed zero
pixels, so this is the guard and the sweep never was. (2) Neither can the visual
sweep: the CRITICAL dispatch fix changed **zero pixels in 34 scenes**,
including the `palette` scene, because a shot captures the popup's layout and
the bug was in what happens when a row is chosen. A green visual gate is
evidence about drawing, not about behaviour.

One warning does remain, and it is specific to the shot build rather than the
release build: with `--features software`, `render()` in
`src/bin/quire_shot.rs:75` takes a `ui` parameter its software path never
reads (`unused variable: ui`). Harmless — but it is the reason the shot
binary's build is not silent even when the app's is.
